$kind: http-request
description: |-
  Updates a workspace's associated tags. This endpoint replaces all existing tags with those you pass in the request body.

  **Note:**

  - You can only add a maximum of five tags to an API.
      
  - Tags must be between 2 and 64 characters long.
      
  - Tags must follow the `^[a-z][a-z0-9-]\\\\\\*[a-z0-9]+$` pattern.
url: "{{baseUrl}}/workspaces/:workspaceId/tags"
method: PUT
pathVariables:
  - key: workspaceId
    value: ""
    description: The workspace's ID.
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "deprecated": false,
            "uniqueItems": false
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
        "tags": [
            {
                "slug": "{{tagName}}"
            }
        ]
    }
  $type: |-
    {
        "type": "object",
        "properties": {
            "tags": {
                "type": "array",
                "minItems": 0,
                "maxItems": 5,
                "description": "A list of the associated tags as slugs.",
                "items": {
                    "type": "object",
                    "description": "Information about the tag.",
                    "properties": {
                        "slug": {
                            "type": "string",
                            "minLength": 2,
                            "maxLength": 64,
                            "pattern": "^[a-z][a-z0-9-]*[a-z0-9]+$",
                            "description": "The tag's ID within a team or individual (non-team) user scope."
                        }
                    }
                }
            }
        }
    }
examples: ./.resources/Update a workspace's tags.resources/examples
order: 2000
