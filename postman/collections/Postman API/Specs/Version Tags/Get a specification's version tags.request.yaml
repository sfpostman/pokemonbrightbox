$kind: http-request
description: Gets a list of a specification's version tags.
url: "{{baseUrl}}/specs/:specId/version-tags"
method: GET
queryParams:
  - key: limit
    value: ""
    disabled: true
    description: The maximum number of rows to return in the response.
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "integer",
            "default": "10"
          }
        }
      }
  - key: cursor
    value: ""
    disabled: true
    description: The pointer to the first record of the set of paginated results. To view the next response, use the `nextCursor` value for this parameter.
    $type: string
pathVariables:
  - key: specId
    value: ""
    description: The specification's ID.
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
examples: ./.resources/Get a specification's version tags.resources/examples
order: 3000
