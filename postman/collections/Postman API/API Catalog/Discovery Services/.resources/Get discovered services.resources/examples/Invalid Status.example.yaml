$kind: http-example
request:
  url: https://api.getpostman.com/api-catalog/discovery-services?status=sample
  method: GET
  queryParams:
    status: sample
response:
  statusCode: 400
  statusText: Bad Request
  body:
    type: json
    content: |-
      {
          "type": "https://api.postman.com/problems/bad-request",
          "status": 400,
          "title": "Bad Request",
          "detail": "status values must be one of: discovered, archived, integrated",
          "errors": [
              {
                  "path": "status",
                  "message": "status values must be one of: discovered, archived, integrated"
              }
          ]
      }
order: 7000
