$kind: http-request
description: |-
  Updates a comment on a response.

  **Note:**

  This endpoint accepts a max of 10,000 characters.
url: "{{baseUrl}}/collections/:collectionUid/responses/:responseUid/comments/:commentId"
method: PUT
pathVariables:
  - key: collectionUid
    value: ""
    description: The collection's unique ID.
  - key: responseUid
    value: ""
    description: The response's unique ID.
  - key: commentId
    value: ""
    description: The comment's ID.
body:
  type: json
  content: |-
    {
        "body": "{{comment}}",
        "tags": {
            "{{userName}}": {
                "type": "user",
                "id": "{{userId}}"
            }
        }
    }
examples: ./.resources/Update a response's comment.resources/examples
order: 3000
