$kind: http-request
url: "{{baseUrl}}/types"
method: GET
headers:
  Accept: application/json
body:
  type: text
  content: ""
examples: ./.resources/List all types.resources/examples
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });
order: 1000
