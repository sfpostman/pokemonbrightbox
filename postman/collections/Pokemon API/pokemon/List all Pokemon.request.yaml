$kind: http-request
url: "{{baseUrl}}/pokemon?limit=20&offset=0"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: limit
    value: "20"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "integer",
            "default": 20,
            "minimum": 1,
            "maximum": 100
          }
        }
      }
  - key: offset
    value: "0"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "integer",
            "default": 0,
            "minimum": 0
          }
        }
      }
body:
  type: text
  content: ""
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
examples: ./.resources/List all Pokemon.resources/examples
order: 1000
